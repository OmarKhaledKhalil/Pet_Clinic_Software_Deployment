pipeline {
    agent any

    environment {
        TF_DIR = "petclinic-infra/terraform"
        ANSIBLE_DIR = "petclinic-infra/ansible"
        JENKINS_IP = "147.161.245.79"
    }

    stages {

        stage('Checkout Code') {
            steps {
                git url: 'https://github.com/OmarKhaledKhalil/Pet_Clinic_Software_Deployment.git', branch: 'master'
            }
        }

        stage('Terraform Init, Plan & Apply') {
            steps {
                dir("${TF_DIR}") {
                    withCredentials([
                        string(credentialsId: 'aws-access-key-id', variable: 'AWS_ACCESS_KEY_ID'),
                        string(credentialsId: 'aws-secret-access-key', variable: 'AWS_SECRET_ACCESS_KEY'),
                        string(credentialsId: 'aws-session-token', variable: 'AWS_SESSION_TOKEN')
                    ]) {
                        sh '''
                            echo "🔐 Exporting AWS credentials"
                            export AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
                            export AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
                            export AWS_SESSION_TOKEN=$AWS_SESSION_TOKEN

                            echo "🚀 Running Terraform with Jenkins IP: ${JENKINS_IP}"
                            terraform init
                            terraform plan \
                                -var="key_pair=jenkins-key" \
                                -var="jenkins_host_ip=${JENKINS_IP}" \
                                -var="ci_host_ip=${JENKINS_IP}"
                            terraform apply -auto-approve \
                                -var="key_pair=jenkins-key" \
                                -var="jenkins_host_ip=${JENKINS_IP}" \
                                -var="ci_host_ip=${JENKINS_IP}"
                        '''
                    }
                }
            }
        }

        stage('Wait for EC2 Boot') {
            steps {
                echo "⏳ Waiting 100 seconds for EC2 instances to boot..."
                sleep time: 100, unit: 'SECONDS'
            }
        }

        stage('Generate Ansible Inventory') {
            steps {
                dir("${ANSIBLE_DIR}") {
                    withCredentials([sshUserPrivateKey(credentialsId: 'ansible-ssh-key', keyFileVariable: 'SSH_KEY')]) {
                        sh '''
                            echo "🔑 Preparing SSH Key"
                            cp $SSH_KEY ./jenkins-key.pem
                            chmod 400 ./jenkins-key.pem

                            echo "⚙️ Running inventory generator script"
                            chmod +x generate_inventory.sh
                            ./generate_inventory.sh ./jenkins-key.pem

                            echo "📂 Generated inventory:"
                            cat inventory/hosts.ini
                        '''
                    }
                }
            }
        }

        stage('Run Ansible Playbook') {
            steps {
                dir("${ANSIBLE_DIR}") {
                    withCredentials([sshUserPrivateKey(credentialsId: 'ansible-ssh-key', keyFileVariable: 'SSH_KEY')]) {
                        sh '''
                            KEY_PATH="$WORKSPACE/jenkins-key.pem"
                            echo "📂 Checking key file and inventory:"
                            ls -l "$KEY_PATH"
                            cat inventory/hosts.ini

                            echo "📡 Starting SSH agent and adding key"
                            eval "$(ssh-agent -s)"
                            ssh-add "$KEY_PATH"

                            export SSH_AUTH_SOCK=$SSH_AUTH_SOCK
                            echo "✅ SSH_AUTH_SOCK is: $SSH_AUTH_SOCK"

                            echo "📡 Running Ansible playbook"
                            ansible-playbook -i inventory/hosts.ini site.yml --private-key "$KEY_PATH" --forks 1
                        '''
                    }
                }
            }
        }
    }

    post {
        success {
            echo "✅ Infrastructure and Kubernetes setup completed successfully."
        }
        failure {
            echo "❌ Pipeline failed. Check logs for details."
        }
    }
}
